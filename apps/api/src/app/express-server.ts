import { expressServer } from './express-server'
import * as express from 'express';
import * as path from 'path';
import * as cors from 'cors';

const es = express()

expressServer.use()

export const es as expressServer